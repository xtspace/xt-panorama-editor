
export const enum LogoTypeEnum {
  NONE = 1,
  CUSTOM = 2,
}

export const enum LogoLinkEnum {
  NONE = 1,
  OPEN = 2,
}

export const enum LogoLinkTypeEnum {
  NEW = 1,
  CURRENT = 2,
  POP = 3
}

export const enum FormItemEnum {
  INPUT = "input",
  SELECT = "select",
  TEXTAREA = "textarea"
}

export const enum FormTypeEnum {
  BASE = 'base',
  LOGO = 'logo'
}